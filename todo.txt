todo: дважды server run или stop, когда сервер запускается
devmod в конфиге(web файлы загружаются заново)
при наборе exit js блочит навсегда ввод в consoleLog из другого потока
редкие key mistmatch связаны думаю с многопоточностью, а смена ключей же последовательна. наверно попадает между checkAdminSession и sendNews.
log.print читает adminSession, и вот вышесказанная проблема. нужно как-то пофиксить
ограничение кол-ва строк в consoleLog, обрезание старья и прокрутка вниз(если она сейчас внизу). по логике надо: если длина буфера n, а вывода m, то надо создать новый буфер substring(m,n-m)+msg
состояния сервера. вроде бы единственное состояние в приложении.
хм, можно сделать так, чтобы опрашиватель отсылал бы команды, если такие были бы на очереди, а иначе arenews. тогда все будет синхронно.
    типа if input {while(input) {send, readNews} }else{arenews}
logout
пароль должен храниться в config
пакетный менеджер и менюшки
